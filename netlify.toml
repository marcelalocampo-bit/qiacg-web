[build]
  command = "npm run build && mkdir -p dist/functions/server && cp -r dist/server/* dist/functions/server/"
  publish = "dist/client"

[functions]
  directory = "dist/functions"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
