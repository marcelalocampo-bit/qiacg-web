[build]
  command = "npm run build && mkdir -p dist/functions && cp -r .netlify/functions-internal/* dist/functions/"
  publish = "dist/client"

[functions]
  directory = "dist/functions"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
